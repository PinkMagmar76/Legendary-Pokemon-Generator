// Legendary Pokémon list (wheel options) const legendaryPokemon = [ "Mewtwo", "Mew", "Lugia", "Ho-Oh", "Raikou", "Entei", "Suicune", "Regirock", "Regice", "Registeel", "Latias", "Latios", "Kyogre", "Groudon", "Rayquaza", "Jirachi", "Deoxys" ]; // Secret Pokémon (not on wheel) const secretPokemon = "Magmar"; const secretChance = 0.05; // 5% chance const wheel = document.getElementById("wheel"); const result = document.getElementById("result"); const generateBtn = document.getElementById("generateBtn"); // Build wheel slices dynamically function buildWheel() { const sliceCount = legendaryPokemon.length; const angleStep = 360 / sliceCount; legendaryPokemon.forEach((name, i) => { const slice = document.createElement("div"); slice.className = "slice"; slice.style.transform = `rotate(${i * angleStep}deg) skewY(${90 - angleStep}deg)`; slice.textContent = name; wheel.appendChild(slice); }); } buildWheel(); // Spin + generate logic generateBtn.addEventListener("click", () => { result.textContent = ""; // Secret Magmar chance if (Math.random() < secretChance) { result.textContent = "🔥 Secret Pokémon: Magmar!"; spinRandom(); return; } // Pick a random legendary const index = Math.floor(Math.random() * legendaryPokemon.length); const selected = legendaryPokemon[index]; // Spin wheel to selected slice const sliceAngle = 360 / legendaryPokemon.length; const targetAngle = 360 * 5 + (index * sliceAngle); // 5 full spins + landing wheel.style.transform = `rotate(-${targetAngle}deg)`; setTimeout(() => { result.textContent = `⭐ You got: ${selected}!`; }, 4000); }); // Random spin for Magmar (no alignment needed) function spinRandom() { const randomAngle = 360 * 5 + Math.random() * 360; wheel.style.transform = `rotate(-${randomAngle}deg)`; }
